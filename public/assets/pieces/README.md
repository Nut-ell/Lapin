Replace the placeholder puzzle piece SVGs in this folder with the real gem or star assets later.

Suggested approach:

- Keep one file per piece type.
- Reuse the same filenames when possible to avoid code changes.
- If you add or rename pieces, update `src/assets/pieces.ts`.

